# 趴下窗口分析工具 快速使用说明

分析机器人充电趴桩过程的工具：从 `charge_manager` 和 `reflective_column_node` 两份日志，自动生成每个趴下窗口的**左右反光柱坐标 + 机器人定位轨迹**（网页图 + txt 报告），并标注电流检测成败。

只需要一台装了 Python 3 的 Windows/Linux/Mac 电脑，无需安装任何第三方库。

---

## 一、推荐方式：上传网页版（AAA_sit_down_server.py）

最简单，全程在浏览器里点。

### 启动

命令行进入工具目录，运行：

```powershell
python AAA_sit_down_server.py
```

- 会自动打开浏览器 `http://127.0.0.1:8306`（打不开就手动访问这个地址）
- 想换端口：`python AAA_sit_down_server.py 9000`
- **如果报"端口被占用"**：说明有旧的服务器进程残留，关掉之前开过的命令行窗口（或任务管理器结束 python 进程）再启动
- 用完在命令行按 `Ctrl+C` 退出

### 上传分析

1. 页面上两个文件选择框：分别选 `charge_manager` 日志和 `reflective_column_node` 日志
   - 新旧命名都支持：`charge_manager.2026_0907.log` 或 `charge_manager_v5.0.22.2026_0907.log`
   - 必须是**同一天**的两份日志
2. 点"开始分析"，等 1~2 分钟（reflective 日志越大越慢，100MB 约需 2 分钟）
3. 自动跳到结果页；页面里也能直接下载 txt 版报告

### 读图指南（网页版核心功能）

- **左侧列表**：所有趴下窗口。红色"失败(无电流)"置顶，可按 成功/失败 筛选
- **窗口标题行**：三段时刻（精调点 → sit_down_high → 趴下完成）、成败、电流检测记录
- **Y / X 轨迹图**：三条曲线——蓝=左柱、橙=右柱、紫=机器人定位（单位 mm）
  - 绿虚线=精调点、蓝虚线=sit_down_high（开始趴下）、红虚线=趴下完成（Z 压到底，开始测电流）
  - 红线旁的彩色数字 = 趴下完成瞬间三条曲线的值
  - 青绿菱形 ◆ = 第 i 次检测电流时机器人的位置（旁标"检N 数值"）
  - 鼠标在图上左右移动：竖线跟随，实时显示该秒全部数值
- **逐秒明细表**：时刻 / 簇数 / 左柱(x,y) / 右柱(x,y) / 机器人定位(x,y)，"--"表示该秒无数据

**典型失败特征**：趴下完成后（红线右侧）右柱 y 单调下滑直到消失、机器人定位 y 反向漂移（±60~100mm+）→ LOST → 无电流。趴下动作本身（蓝线到红线之间）柱坐标通常不动，成败分水岭在趴完之后。

---

## 二、命令行方式（不开服务器）

```powershell
# 生成网页（同时自动生成同名 .txt）
python sitdown_web.py <charge_manager日志> <reflective_column_node日志> [输出.html]

# 只生成 txt 文本报告
python sitdown_windows.py <charge_manager日志> <reflective_column_node日志> [输出.txt]
```

例：

```powershell
python sitdown_web.py D:\logs\charge_manager.2026_0907.log D:\logs\reflective_column_node.2026_0907.log report.html
```

生成的 html 是**自包含单文件**，双击就能打开，可直接发给同事查看（无需带工具和数据）。

---

## 三、常见问题

| 问题 | 说明 |
|---|---|
| 0 个窗口 | 检查两份日志是否同一天、是否完整（精调点→趴桩测试全过程都在） |
| 机器人定位列全 "--" | 该日志可能是更老版本（无 step 3-3 发布坐标），新版工具会自动回退；全无则该日志确实没记录 |
| "端口被占用" | 有旧服务器进程没关，见上文启动节 |
| 结果页打不开 | 解析还在后台进行，等 1~2 分钟刷新；一次只能解析一个任务 |
| Windows 命令行中文乱码 | 不影响功能，看结果页/文件内容即可 |

## 四、文件清单

| 文件 | 用途 |
|---|---|
| AAA_sit_down_server.py | 上传网页版服务器（推荐入口） |
| sitdown_web.py | 命令行网页生成器（服务器版也依赖它） |
| sitdown_windows.py | 命令行 txt 报告生成器 |
| 使用说明.md | 本文档 |

所有解析在本机完成，日志不会上传到任何外部服务器。
